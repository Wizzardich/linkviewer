# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
POST    /store                          controllers.LinkStorage.store
GET     /links/:uuid                    controllers.LinkStorage.get(uuid: String)


    